# Kiến thức cơ bản về AI, ML, DL

## Machine Learning (Học máy)

**Machine Learning (Học máy)** là một nhánh của Trí tuệ nhân tạo (AI), tập trung vào việc xây dựng các mô hình có thể **học từ dữ liệu** mà không cần được lập trình trực tiếp.

## Phân biệt AI, Machine Learning, Deep Learning

### 1) AI (Trí tuệ nhân tạo)
Là một lĩnh vực rộng, bao gồm mọi hệ thống có thể mô phỏng trí tuệ con người.

### 2) ML - Học máy
- **Một nhánh của AI**
- **Sử dụng thuật toán để:**
  - Học từ dữ liệu
  - Đưa ra phán đoán

### 3) DL (Học sâu)
- **Một phần của ML**
- **Sử dụng mạng nơ ron nhân tạo để:** xử lý dữ liệu phức tạp như hình ảnh, âm thanh, ngôn ngữ

#
# 2. Các loại ML chính

### 1) Học có giám sát
Học từ dữ liệu có dán nhãn.

**Nguyên lý:**

#### 1. Giai đoạn huấn luyện:
- ⇒ Cung cấp 1 lượng lớn dữ liệu có nhãn
- ⇒ Mô hình sẽ học cách nhận diện đặc điểm quan trọng của các tập dữ liệu
- ⇒ Sau khi học xong, ta sẽ thu được mô hình AI

#### 2. Giai đoạn suy luận/ dự đoán:
- Khi có một dữ liệu mới, ta đưa nó vào mô hình AI
- Mô hình sẽ suy nghĩ và trả về nhãn hợp lý nhất.

**Hai bài toán chính:**

- **Phân loại:** chọn một nhãn phù hợp trong các nhãn có sẵn.
- **Hồi quy:** dự đoán một số thực, không giới hạn trong một tập giá trị có định.


### 2) Học không giám sát

Học từ dữ liệu không có nhãn, tự tìm điểm giống nhau giữa các dữ liệu và ghép nhóm nó với nhau hoặc tìm ra mối liên hệ giữa các cụm dữ liệu.

**Hai bài toán chính:**

- **Phân cụm:** đưa các tập dữ liệu giống nhau vào 1 cụm.
- **Luật kết hợp:** Tìm ra mối quan hệ giữa các mục dữ liệu trong tập dữ liệu lớn.

### 3) Học tăng cường

Học thông qua thử và sai.


# 3. Các khái niệm quan trọng

### 1. Overfitting (quá khớp): "Học vẹt"

Chỉ nhận diện mạnh với dữ liệu đã train, không nhận diện được dữ liệu mới.

**Ví dụ:**

- Bạn chỉ học thuộc lòng bài văn mẫu mà không hiểu nội dung. Gặp đề bài hơi khác là tạch.
- Mô hình AI nhận diện chó mèo, nhưng nó lại nhớ cả background phía sau con mèo luôn. Kết quả: chỉ cần đổi nền là "Ớ con gì đây???"

### 2. Underfitting (chưa khớp): nước đổ lá khoai. Học như không học, toán đoán mò.

- Học lý thuyết nửa vời, vào bài kiểm tra thì cái gì cũng đoán "C".
- Mô hình AI nhận diện mèo mà chỉ nhìn tai, thế là chó husky cũng bảo là mèo.


### 3. Bias-Variance Tradeoff:

- **Bias cao:** mô hình đơn giản ⇒ Underfitting
- **Variance cao:** mô hình quá phức tạp ⇒ Overfitting
  - ⇒ **Mục tiêu:** Cân bằng giữa Bias và Variance để mô hình học vừa đủ, không ngu mà cũng không học vẹt.

**Hình dung dễ hiểu:**

- Bias cao giống như học sinh đốt, đọc đề cái là chọn bừa.
- Variance cao giống như học sinh mọt sách nhưng chỉ học tủ, trúng tủ thì điểm cao, lệch tủ cái là bay màu.
- Cần một học sinh vừa giỏi, vừa linh hoạt → Đây là mô hình lý tưởng.


# 4. Hồi Quy Tuyến Tính (Linear Regression)

### Quy trình của Hồi quy tuyến tính

#### 1. Khởi tạo tham số
- Khởi tạo trọng số w và b (thường là 0 hoặc giá trị ngẫu nhiên).

#### 2. Tính toán đầu ra dự đoán
- Dự đoán y^ bằng công thức hồi quy tuyến tính:
  ```
  y^ = wx + b
  ```

#### 3. Tính hàm mất mát (Loss Function)
- Dùng **Mean Squared Error (MSE)** để đo độ sai lệch giữa giá trị dự đoán và giá trị thực tế:
  ```
  L = (1/m) Σ(yi - y^)²
  ```
  - **yi** là giá trị y đã dán nhãn
  - **→** Nếu L càng nhỏ, mô hình càng tốt.

#### 4. Tính Gradient (Đạo hàm) để cập nhật tham số
- Tính đạo hàm theo w và b để biết phải di theo hướng nào:
  ```
  ∂L/∂w = (2/m) Σ xi(yi - ŷi)
  ∂L/∂b = (2/m) Σ(yi - ŷi)
  ```

#### 5. Cập nhật w, b bằng Gradient Descent
- Dùng công thức Gradient Descent để điều chỉnh giá trị w và b:
  ```
  w := w - α × (∂L/∂w)
  b := b - α × (∂L/∂b)
  ```
  - **α** (alpha): learning rate - tốc độ học

#### 6. Kiểm tra hội tụ
- Nếu sai số còn lớn, lặp lại từ bước 2 → 5.
- Nếu sai số nhỏ hoặc đạt số vòng lặp tối đa, dừng lại.

### Giải thích các thành phần

**Trong đó:**
- **w** (weight): trọng số, hệ số góc của đường thẳng
- **b** (bias): hệ số chặn, điểm cắt trục y
- **x**: biến đầu vào (input)
- **y**: giá trị thực tế (actual value)
- **ŷ** (y^): giá trị dự đoán (predicted value)
- **L**: Loss (hàm mất mát)
- **m**: số lượng mẫu dữ liệu
- **α**: learning rate (tốc độ học)



# 5. Cây Quyết Định (Decision Tree)

### Khái niệm

Nó kiểu như một cái sơ đồ tư duy không lỏ, nhưng thay vì tóm tắt bài học, nó dùng để ra quyết định.

Mỗi cái nút (node) trong cây là một câu hỏi kiểu:

👉 "Dữ liệu này có lớn hơn ngưỡng X không?"
👉 "Thu nhập của khách hàng trên 10 triệu không?"
👉 "Con mèo này có màu đen không?"

Dựa vào câu trả lời (có/không), ta đi xuống các nhánh khác nhau, cho đến khi **chốt đơn** ở một nút lá – chính là kết quả dự đoán

### Điểm hay ho?

- Không cần phải hiểu sâu về xác suất thống kê, chỉ cần **chặt chém dữ liệu theo kiểu "đúng thì đi lối này, sai thì đi lối kia"**.
- Muốn dùng? Gọi hàm `DecisionTreeClassifier()` là xong, huấn luyện nhanh, predict gọn.

### Điểm dở?

- Cây càng to, càng dễ bị **overfitting** – tức là học vẹt theo dữ liệu cũ, gặp dữ liệu mới là toang.


# 6. Mạng Nơ Ron (Neural Network)

### Hiểu nơ-ron nhân tạo theo kiểu đơn giản:

- Nó chỉ là một cái hộp, nhận đầu vào, nhân với một đống số (trọng số), cộng thêm tí b (bias), rồi quăng qua một hàm kích hoạt.

- Xong xuôi, cái hộp này truyền kết quả cho cái hộp khác, cứ thế chồng chất lên nhau thành cả một đội hình deep learning.

- Nếu đầu ra sai bét? Không sao, ta có **backpropagation** – một cơ chế tự vả vào mặt để chỉnh sửa trong số và học từ lỗi lầm.

### Vậy nên, nếu chỉ cần dùng mạng nơ-ron, ta làm gì?

👉 Gọi hàm! Cứ `model.add(Dense(64, activation='relu'))` là xong! Không cần biết chuyện gì xảy ra bên trong, nó vẫn chạy ngon.
